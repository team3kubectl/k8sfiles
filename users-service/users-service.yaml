apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: team3-ns
spec:
  selector:
    app: users-service

  ports:
    - port: 8001
      targetPort: 8001
      protocol: TCP

  type: ClusterIP