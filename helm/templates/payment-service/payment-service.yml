apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: team3-ns
spec:
  type: ClusterIP
  selector:
    app: payment-deploy
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004