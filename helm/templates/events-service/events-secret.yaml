apiVersion: v1
kind: Secret
metadata:
  name: events-secret
  namespace: team3-ns
type: Opaque
stringData:
  MONGO_USER: {{ .Values.events.secrets.mongoUser | quote }}
  MONGO_PASSWORD: {{ .Values.events.secrets.mongoPassword | quote }}
