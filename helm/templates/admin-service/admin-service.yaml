apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: team3-ns
spec:
  selector:
    app: admin-service
  ports:
    - port: 8008
      targetPort: 8008
      protocol: TCP
  type: ClusterIP