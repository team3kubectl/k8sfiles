{{- if .Values.services.events.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: events-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.events.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
