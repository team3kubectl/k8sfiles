{{- if .Values.services.users.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: users-config
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.services.users.env }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
{{- end }}
