{{- if .Values.services.users.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: users-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.users.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
