{{- if .Values.services.ai.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: ai-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.ai.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
