{{- if .Values.services.admin.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-config
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.services.admin.env }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
{{- end }}
