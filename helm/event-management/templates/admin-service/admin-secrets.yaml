{{- if .Values.services.admin.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: admin-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.admin.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
