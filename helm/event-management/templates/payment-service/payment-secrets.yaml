{{- if .Values.services.payment.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: payment-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.payment.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
