{{- if .Values.services.booking.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: booking-config
  namespace: {{ .Values.global.namespace }}
data:
  {{- range $key, $value := .Values.services.booking.env }}
  {{ $key }}: "{{ $value }}"
  {{- end }}
{{- end }}
