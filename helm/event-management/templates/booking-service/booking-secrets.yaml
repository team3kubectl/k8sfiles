{{- if .Values.services.booking.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: booking-secret
  namespace: {{ .Values.global.namespace }}
type: Opaque
data:
  {{- range $key, $value := .Values.services.booking.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
